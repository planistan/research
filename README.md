# Planistan Research

This repository is a Jekyll site intended for GitHub Pages deployment.

## Local run

```bash
bundle install
bundle exec jekyll serve
```

## GitHub Pages

Use the workflow in `.github/workflows/jekyll.yml`.
In repository settings, set Pages to build/deploy from **GitHub Actions**.
